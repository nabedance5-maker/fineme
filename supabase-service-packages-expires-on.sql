-- ✅ 本番適用済 2026-09-16
-- 「パッケージを作る」の有効期限を、購入からの日数だけでなくカレンダーから
-- 絶対日付でも設定できるようにする（でお要望2026-09-16：「有効期限はカレンダーから
-- 選択していつまでって設定できるように」）。季節キャンペーン券のように、
-- 購入日に関わらず全員同じ日に切れるチケットに対応するため。
-- validity_daysと併用不可（どちらか一方）。expires_onがあれば優先する。
ALTER TABLE public.service_packages ADD COLUMN IF NOT EXISTS expires_on DATE;
